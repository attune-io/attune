apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: requests-only
spec:
  description: Verify that a AttunePolicy with controlledValues RequestsOnly is accepted and reconciles
  timeouts:
    apply: 30s
    assert: 2m
    delete: 30s
  steps:
    - name: create-resources
      try:
        - apply:
            resource:
              apiVersion: v1
              kind: Namespace
              metadata:
                name: e2e-requests-only
        - apply:
            resource:
              apiVersion: apps/v1
              kind: Deployment
              metadata:
                name: reqonly-app
                namespace: e2e-requests-only
              spec:
                replicas: 1
                selector:
                  matchLabels:
                    app: reqonly-app
                template:
                  metadata:
                    labels:
                      app: reqonly-app
                  spec:
                    containers:
                      - name: app
                        image: registry.k8s.io/pause:3.9
                        resources:
                          requests:
                            cpu: 500m
                            memory: 512Mi
                          limits:
                            cpu: 500m
                            memory: 512Mi
        - apply:
            resource:
              apiVersion: attune.io/v1alpha1
              kind: AttunePolicy
              metadata:
                name: reqonly-test
                namespace: e2e-requests-only
              spec:
                targetRef:
                  kind: Deployment
                  name: reqonly-app
                metricsSource:
                  prometheus:
                    address: http://prometheus-server.monitoring:80
                  minimumDataPoints: 1
                  historyWindow: 1h
                cpu:
                  percentile: 95
                  overhead: "20"
                  controlledValues: RequestsOnly
                memory:
                  percentile: 99
                  overhead: "30"
                  controlledValues: RequestsOnly
                updateStrategy:
                  type: Recommend
                  cooldown: 1m
    - name: verify-deployment-ready
      try:
        - assert:
            resource:
              apiVersion: apps/v1
              kind: Deployment
              metadata:
                name: reqonly-app
                namespace: e2e-requests-only
              status:
                readyReplicas: 1
    - name: verify-policy-reconciles
      try:
        - assert:
            resource:
              apiVersion: attune.io/v1alpha1
              kind: AttunePolicy
              metadata:
                name: reqonly-test
                namespace: e2e-requests-only
              status:
                workloads:
                  discovered: 1
  catch:
    - describe:
        apiVersion: attune.io/v1alpha1
        kind: AttunePolicy
        namespace: e2e-requests-only
    - podLogs:
        namespace: attune-system
        selector: app.kubernetes.io/name=attune
