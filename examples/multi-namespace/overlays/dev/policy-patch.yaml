# Dev: aggressive settings, fast feedback loop
apiVersion: rightsize.io/v1alpha1
kind: RightSizePolicy
metadata:
  name: api-servers
spec:
  cpu:
    percentile: 90
    safetyMargin: "1.1"
  memory:
    percentile: 95
    safetyMargin: "1.1"
  updateStrategy:
    type: Auto
    cooldown: 30m
    maxCpuChangePercent: 100
    maxMemoryChangePercent: 100