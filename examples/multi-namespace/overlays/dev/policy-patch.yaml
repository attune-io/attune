# Dev: aggressive settings, fast feedback loop
apiVersion: attune.io/v1alpha1
kind: AttunePolicy
metadata:
  name: api-servers
spec:
  cpu:
    percentile: 90
    overhead: "10"
    maxChangePercent: 100
  memory:
    percentile: 95
    overhead: "10"
    maxChangePercent: 100
  updateStrategy:
    type: Auto
    cooldown: 30m