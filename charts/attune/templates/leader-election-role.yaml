{{- if .Values.leaderElection.enabled }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "attune.fullname" . }}-leader-election
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "attune.labels" . | nindent 4 }}
rules:
  - apiGroups:
      - coordination.k8s.io
    resources:
      - leases
    resourceNames:
      - attune.attune.io
    verbs:
      - get
      - update
      - delete
  - apiGroups:
      - coordination.k8s.io
    resources:
      - leases
    verbs:
      - create
  - apiGroups:
      - ""
    resources:
      - events
    verbs:
      - create
      - patch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "attune.fullname" . }}-leader-election
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "attune.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "attune.fullname" . }}-leader-election
subjects:
  - kind: ServiceAccount
    name: {{ include "attune.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
{{- end }}