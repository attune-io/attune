{{- if .Values.metrics.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "kube-rightsize.fullname" . }}-metrics
  labels:
    {{- include "kube-rightsize.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.metrics.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "kube-rightsize.selectorLabels" . | nindent 4 }}
{{- end }}
