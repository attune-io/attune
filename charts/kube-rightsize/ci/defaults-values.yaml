---
# CI test: defaults enabled
defaults:
  enabled: true
  updateStrategy:
    type: Recommend
    cooldown: "1h"
    autoRevert: true
    resizeMethod: InPlaceOnly
    maxConcurrentResizes: 1
webhooks:
  enabled: false
