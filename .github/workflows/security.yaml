name: Security

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch: {}
  schedule:
    - cron: "0 6 * * 1"  # Weekly Monday 6am UTC

permissions:
  contents: read
  pull-requests: read

concurrency:
  group: security-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  # CodeQL requires GitHub Advanced Security (paid) for private repos.
  # Re-enable if the repo becomes public or GHAS is purchased.
  govulncheck:
    name: Govulncheck
    runs-on: self-hosted
    timeout-minutes: 10
    steps:
      - uses: step-security/harden-runner@ab7a9404c0f3da075243ca237b5fac12c98deaa5 # v2
        with:
          egress-policy: audit
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6
        with:
          go-version: "1.26"
          cache: false

      - name: Run govulncheck
        shell: bash -Eeuo pipefail -x {0}
        run: |
          go install golang.org/x/vuln/cmd/govulncheck@v1.3.0
          govulncheck ./...

  trivy:
    name: Trivy
    runs-on: self-hosted
    timeout-minutes: 10
    steps:
      - uses: step-security/harden-runner@ab7a9404c0f3da075243ca237b5fac12c98deaa5 # v2
        with:
          egress-policy: audit
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - uses: ./.github/actions/setup-clean-docker-config

      - name: Trivy filesystem scan
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          scan-type: fs
          scan-ref: .
          scanners: vuln
          severity: HIGH,CRITICAL
          exit-code: 1
          ignore-unfixed: true

  trivy-image:
    name: Trivy Image Scan
    runs-on: self-hosted
    timeout-minutes: 15
    steps:
      - uses: step-security/harden-runner@ab7a9404c0f3da075243ca237b5fac12c98deaa5 # v2
        with:
          egress-policy: audit
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6
        with:
          go-version: "1.26"
          cache: false

      - uses: ./.github/actions/setup-clean-docker-config

      - name: Build image for scanning
        shell: bash -Eeuo pipefail -x {0}
        run: |
          scan_tar="${RUNNER_TEMP:-/tmp}/kube-rightsize-scan-${GITHUB_RUN_ID}-${GITHUB_JOB}.tar"
          # Export directly to tarball via buildx. Avoids "docker save" which
          # can fail with "No such image" when Docker uses the containerd image
          # store (default in Docker Desktop v29+).
          docker buildx build \
            --output "type=docker,dest=$scan_tar" \
            -t kube-rightsize:scan .

      - name: Trivy image scan
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          input: ${{ runner.temp }}/kube-rightsize-scan-${{ github.run_id }}-${{ github.job }}.tar
          severity: HIGH,CRITICAL
          exit-code: 1
          ignore-unfixed: true

      - name: Cleanup scan artifacts
        if: always()
        shell: bash -Eeuo pipefail {0}
        run: rm -f "${RUNNER_TEMP:-/tmp}/kube-rightsize-scan-${GITHUB_RUN_ID}-${GITHUB_JOB}.tar"

  gitleaks:
    name: Gitleaks
    runs-on: self-hosted
    timeout-minutes: 10
    steps:
      - uses: step-security/harden-runner@ab7a9404c0f3da075243ca237b5fac12c98deaa5 # v2
        with:
          egress-policy: audit
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          fetch-depth: 0

      - name: Install gitleaks
        shell: bash -Eeuo pipefail -x {0}
        run: |
          export PATH="$HOME/.local/bin:$PATH"
          VER="8.30.1"
          # Verify the cached binary actually runs (catches arch mismatch)
          if command -v gitleaks &>/dev/null && gitleaks version &>/dev/null; then
            echo "gitleaks already installed: $(gitleaks version)"
          else
            mkdir -p "$HOME/.local/bin"
            OS="$(uname -s | tr '[:upper:]' '[:lower:]')"
            ARCH="$(uname -m)"
            case "$ARCH" in
              x86_64)  ARCH="x64" ;;
              aarch64|arm64) ARCH="arm64" ;;
            esac
            curl -sfL "https://github.com/gitleaks/gitleaks/releases/download/v${VER}/gitleaks_${VER}_${OS}_${ARCH}.tar.gz" \
              | tar xz -C "$HOME/.local/bin" gitleaks
          fi

      - name: Detect secrets
        shell: bash -Eeuo pipefail -x {0}
        run: |
          export PATH="$HOME/.local/bin:$PATH"
          gitleaks detect --source . --exit-code 1

  # Dependency Review requires GitHub Advanced Security (paid) for private repos.
  # Trivy and Govulncheck already cover dependency vulnerability scanning.
  # Re-enable if the repo becomes public or GHAS is purchased.
  # dependency-review:
  #   name: Dependency Review
  #   runs-on: self-hosted
  #   if: github.event_name == 'pull_request'
  #   steps:
  #     - uses: step-security/harden-runner@ab7a9404c0f3da075243ca237b5fac12c98deaa5 # v2
  #       with:
  #         egress-policy: audit
  #     - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
  #     - uses: actions/dependency-review-action@v5
  #       with:
  #         fail-on-severity: high
